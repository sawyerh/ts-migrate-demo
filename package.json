{
  "name": "ts-migrate-demo",
  "version": "1.0.0",
  "scripts": {
    "migrate-jsdoc": "ts-migrate migrate . --plugin jsdoc"
  },
  "devDependencies": {
    "ts-migrate": "^0.1.26",
    "typescript": "^4.4.4"
  }
}
